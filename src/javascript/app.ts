class App {
  constructor () {
    // this.startApp()
  }

  // static rootElement = document.getElementById('root')
  // static loadingElement = document.getElementById('loading-overlay')

  async startApp () {
    // try {
      App.loadingElement.style.visibility = 'visible'

      // const fighters = await fighterService.getFighters()

      // const fightView = new FightView()
      // this.fightersView = new FightersView(fighters) 
      // this.messageBox = new MessageBox('Drag fighters to arena and click <b>Fight</b> button. <br> Click a fighter\'s portrait to edit fighter')
      
  //     const fightViewElement = fightView.element
  //     const messageBoxElement = this.messageBox.element
  //     const fightersElement = this.fightersView.element


  //     App.rootElement.appendChild(fightViewElement)
  //     App.rootElement.appendChild(messageBoxElement)
  //     App.rootElement.appendChild(fightersElement)
  //   } catch (error) {
  //     console.warn(error)
  //     App.rootElement.innerText = 'Failed to load data'
  //   } finally {
  //     App.loadingElement.style.visibility = 'hidden'
    // }
  }
}

export default App
